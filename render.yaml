services:
  - type: web
    name: jobportal-backend
    env: node
    buildCommand: |
      cp package.production.json package.json &&
      npm install &&
      npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
